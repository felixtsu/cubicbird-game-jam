namespace gamejam {

    let counter = 0

    //%block
    export function roomFinished(win:boolean) {
        game.over(win)
        // if (counter == 0) {
        //     foobar2.init()
        //     counter += 1
        // } else {
        //     game.over(win)
        // }
    }

}
